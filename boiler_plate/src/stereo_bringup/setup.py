from setuptools import setup

package_name = 'stereo_bringup'

setup(
    name=package_name,
    version='0.0.1',
    packages=[],
    install_requires=['setuptools'],
    zip_safe=True,
    description='Bringup launch files for stereo vision pipeline',
)
